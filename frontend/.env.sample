# Frontend environment sample for Twiller
# Copy this file to .env.local (or .env.development / .env.production) and fill values.
# Do NOT commit real secrets to the repository. Keep secret keys only in backend/.env or CI.

# Backend API base (must include /api when calling the backend endpoints)
REACT_APP_API_URL=http://localhost:5000/api

# Stripe publishable key (public) — keep secret key only on backend
REACT_APP_STRIPE_PUBLISHABLE_KEY=

# Optional Firebase config (if used)
REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=

# Optional defaults
REACT_APP_DEFAULT_THEME=light